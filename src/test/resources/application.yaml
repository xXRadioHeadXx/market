spring:
  datasource:
    url: "jdbc:h2:mem:AZ;MODE=PostgreSQL;DB_CLOSE_ON_EXIT=FALSE;"
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect